import { NotificationNormal } from './notification-normal';

describe('NotificationNormal', () => {
  it('should create an instance', () => {
    expect(new NotificationNormal()).toBeTruthy();
  });
});
